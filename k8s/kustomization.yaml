apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - services.yaml
  - deployment.yaml
  - goatcounter.yaml
  - redis.yaml
  - middleware.yaml
  - ingress.yaml
secretGenerator:
  - name: gtcd-env
    envs:
      - gtcd.env
images:
  - name: gtcd
    newTag: local
